filetype: env

detect:
    filename: "(\\.env|\\.env\\..+)$"

rules:
    - comment: "(^#.*)"
    - constant.string: "\"[^\"]*\"|'[^']*'"
    - identifier: "^[A-Za-z_][A-Za-z0-9_]*"
    - symbol.operator: "="
